npx lint-staged
#npm run test -- --passWithNoTests

# .git/hooks/pre-commit
# comentado para facilitar subir commits
if git diff --cached --name-only | grep -q 'schema.prisma'; then
    echo "### ATENÇÃO: Para modificações no schema.prisma solicite diretamente ao gestor do backend."
    exit 1
fi
